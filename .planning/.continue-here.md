---
context: default
phase: none (non-GSD project; tracked via CLAUDE.md + NEXT_STEPS.md)
task: paper de-overwriting + submission staging
status: paused
last_updated: 2026-06-16T13:48:55.109Z
---

# Resume handoff — QLNN ODE/PDE benchmark paper

> This repo does NOT use GSD `.planning/phases/` scaffolding. Project state
> lives in `CLAUDE.md`, `NEXT_STEPS.md`, and `ADVISOR_BRIEF.md`. Read those.

<current_state>
Paper-cleanup session complete. Working tree is CLEAN; HEAD = origin/master =
`bcbad5d`. Four commits this session, all pushed. No code/paper edits pending.

The paper (25pp main + 8pp supplement) is publishable on the minimum path
right now: a complete, pre-registered, integrity-gated H1 FALSIFICATION backed
by 405 result cells (0 errors). The only open items are human decisions, not
work: coauthor sign-off on the trimmed prose, and the minimum-vs-strengthened
submission-path call.
</current_state>

<completed_work>

- baa1a27 — Trimmed overwritten abstract (~330→~175 words) + introduction
  (~halved): methodology relocated to Methods, contributions folded to prose.
- bfc925c — De-overwrote body sections 2–8 via 7 parallel subagents
  (prose-only; every number, `\cite`, `\label`, figure, table preserved and
  centrally verified). Section line counts: 02 411→404, 03 422→374,
  04 415→394, 05 441→387, 06 282→247, 07 311→256, 08 89→54.
- 380df89 — Refreshed stale page counts in CLAUDE.md (17/7 → 25/8).
- bcbad5d — Added PRX Quantum cover letter (`COVER_LETTER.md`, stage-ready,
  NOT submitted).

All gates green throughout: main build exit-0 (25pp), supplement exit-0 (8pp),
no undefined refs/cites, `verify_paper_integrity.py` exit-0 (no numbers
changed anywhere).
</completed_work>

<remaining_work>

- Coauthor reviews trimmed abstract+intro+body for voice/density approval.
- DECISION (yours + coauthor's): minimum-path submission (submit as-is with
  the A15–A22 audit re-runs documented-but-deferred) vs strengthened path
  (run the re-runs on Anvil first, then refresh verdict magnitudes).
- If strengthened: NEXT_STEPS.md Phase A→B→C→D (ACCESS → Anvil → re-run sweep
  → verdict refresh). Phase A is the only hard blocker (advisor letter).
- Optional/reversible: restore the rf_qrc/Lorenz secondary sub-finding to
  conclusions (sec 8) if the coauthor prefers it kept there.
</remaining_work>

<decisions_made>

- Relocated (not deleted) methodology from intro → Methods, after verifying
  Methods already carries the BAS commitments, the methodological-lineage
  detail, and the deferred Kuramoto/KdV extensions.
- Folded the four intro contributions from a numbered list into prose (user
  choice).
- Conclusions dropped the rf_qrc/Lorenz secondary sub-finding to keep the
  single-sub-finding (te_qpinn_qnn/FHN) headline framing consistent with the
  abstract+intro; rf_qrc remains in the body. Reversible.
- Body sweep used 7 parallel subagents with hard invariants; orchestrator
  built + integrity-checked centrally (distinct files → no edit conflicts;
  central build avoids LaTeX aux races).
</decisions_made>

<blockers>
- Phase A ACCESS allocation needs a one-paragraph advisor letter on
  institutional letterhead → blocks ONLY the strengthened-path re-runs
  (Phase C). Minimum-path submission is not blocked by it.
- Submission framing (the FALSIFIED headline) needs coauthor sign-off.
</blockers>

## Required Reading (in order)
1. `CLAUDE.md` — project state + "What NOT to do" (do NOT run re-runs on
   laptop; do NOT push to GitHub `main`; do NOT touch locked integrity
   numbers or the pre-registration without an amendment).
2. `NEXT_STEPS.md` — five-phase path to PRX Quantum submission; the
   minimum-vs-strengthened decision point.
3. `ADVISOR_BRIEF.md` — plain-language narrative + headline finding.
4. `COVER_LETTER.md` + `ARXIV_SUBMISSION.md` — submission package state.

## Critical Anti-Patterns (do NOT repeat these)
- Do NOT run the M3 / A15–A22 audit re-runs on the laptop — they belong on
  Anvil GPU (CLAUDE.md). The runner is wired (`--confirm`) but compute is
  intentionally deferred.
- Do NOT edit `verify_paper_integrity.py` locked numbers — they gate the
  current committed results; the refresh is Phase D's job.
- Do NOT push to GitHub `main` (placeholder) — push to `master`.
- When trimming prose, never drop a `\cite`/`\label`/number — verify invariant
  counts after edits and rebuild + run the integrity gate centrally.

## Infrastructure State
- Git: clean, HEAD `bcbad5d` = origin/master. Nothing uncommitted.
- Results: 405 `metrics.json`, 0 `error.json`. Integrity gate exit-0.
- Build: `bash paper/build.sh` (25pp) + `bash paper/build_supplement.sh` (8pp)
  both exit-0.

<context>
Pure paper-editing session driven by the coauthor's "super overwritten"
feedback (abstract "ALOT", intro "way too much methodology"). The science
and numbers are untouched and locked; this was density/voice work only.
Nothing is mid-flight — the next move is a human decision about submission
path, not more editing.
</context>

<next_action>
Start with: confirm the coauthor is happy with the trimmed density, then make
the minimum-vs-strengthened submission-path decision. If minimum path: the
only gate left is advisor sign-off on the FALSIFIED framing, then follow
COVER_LETTER.md + ARXIV_SUBMISSION.md checklists. If strengthened: begin
NEXT_STEPS.md Phase A (advisor letter for ACCESS).
</next_action>
